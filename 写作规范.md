# 写作规范（Quick Reference）

> 权威源头是 [需求文档.md §6](./需求文档.md)。本文件是每天写作时的**速查卡**，短 + 可扫。

## 两条铁律（最高优先级）

### 铁律一：新概念即时解释

任何读者可能不知道的 **术语 / 按键 / 符号 / 路径 / 缩写 / 界面元素**，首次出现就地解释。

自检：一个没用过电脑专业功能的亲戚读到这段会卡吗？会就补。

### 铁律二：前 5 章颗粒度最细

第 0-4 章适用加强规则——每一步都写出、每一步都给预期结果、每一步都给失败兜底、Mac/Windows 各走完整步骤不靠类推。

---

## 章节结构颗粒度（防止内容碎片化）

**主线**：3-6 个大节为宜，每节**自成主题、有内在线索**。
**支线**：2-4 个大节，相关内容**合并**而非各自单列。

### 硬原则

- **每节至少 300-400 字**；不足 100 字的内容就该**合并进邻近节**，别单独成节
- **同主题内容必须合并**：比如"ls"和"cd"都是"在文件夹间穿梭"，合一节讲；"退出终端"和"常见报错"都是"收尾知识"，合一节讲
- **相邻短节互相依赖时合并**：比如"什么是 X"和"为什么用 X"之间可以直接写成一个"认识 X"
- **支线最多 4 节**：每节围绕一个清晰主题（比如"两个平台的深度差异"），把多个小点合进来

### 自检问题

写完一章回头看：
- 每节标题单独拿出来，**读者能否一眼看懂这节在讲什么主题**？
- 相邻两节是否有**部分内容可以合并**（如果合并后不矛盾，就合）？
- 有没有节**少于 5 行**？若有，强制合并。
- 支线节标题是"**某个合理命名的主题**"，还是"单一小知识点的技术名"？如果是后者，说明拆得太细。

### 好坏对比

**❌ 坏（零碎）**：
```
1.1 什么是终端
1.2 为什么用终端
1.3 Mac 打开终端
1.4 Windows 打开终端
1.5 认识提示符
1.6 pwd 命令
1.7 ls 命令
1.8 cd 命令
1.9 路径
1.10 退出 + 报错
```

**✅ 好（有主题）**：
```
1.1 认识终端（what + why + 两平台命名）
1.2 打开你的终端（Mac 详解 + Windows 详解）
1.3 第一次使用：提示符和 pwd
1.4 在文件夹间穿梭：ls 和 cd
1.5 理解"路径"
1.6 收尾：退出终端，看懂三个常见报错
```

---

## 章末三件套（每章必须有，顺序固定）

1. **本章小结** —— 5-10 行说人话
2. **动手任务** —— 1-3 个，含预估时间、步骤、成功标志
3. **如果你卡住了** —— 列 2-3 个最可能的问题 + 解决办法

> **为什么不设测验题**：零基础读者"懂了没"由**能不能完成动手任务**校准，干瘪的自测反而像考试、增加认知负担。动手任务就是最好的自测。

---

## 双轨结构：🎯 主线 / 🌿 支线

每章**先主线后支线**，用醒目分隔块隔开。

**主线**：必读核心 + 三件套。读完就合格。
**支线**：延伸、对比、原理、变体、冷门。**不读不影响后续章节**。第一遍建议跳过。

### 写作时判断

| 往 🎯 主线 | 往 🌿 支线 |
|----------|-----------|
| 后续章节会用到 | 只让"用得更溜" |
| 不懂会踩坑 | 只是少一点优雅 |
| 做动手任务必需 | 原理 / 历史 / 开发八卦 |
| 自己平台的最常见路径 | 平台对比、冷门备选 |

### 版面

```
---

> 🎯 **【主线】—— 本章必读核心**
> 读完这部分 + 完成章末任务即可进入下一章。

（主线内容）

## 本章小结
## 动手任务
## 如果你卡住了

---

> 🌿 **【支线】—— 可选深入（学有余力再看）**
> 下面是本章话题的延伸和细节。**不读不影响后续章节**，
> 第一遍读书时可以直接跳过。

（支线内容，不需要三件套）
```

### 质量约束

- 支线质量和主线一样（解释不打折、示例不省略）
- 每个支线小节开头**一句话交代"这段讲什么、什么时候该回头读"**
- **支线总量不超过主线**——如果超过，说明不少内容该升级到主线

---

## 语言风格

| 做 | 不做 |
|----|------|
| 第二人称"你" | "用户"、"读者" |
| 大白话（"改一下"、"让它跑"） | 黑话（"调用"、"实例化"、"序列化"、"执行体"） |
| 技术术语首次配生活化翻译 | 术语裸奔 |
| 英文缩写括号注中文 | CLI / IDE / API / JSON 直接丢 |
| ⚠️ 危险 / 💡 贴士 / 📌 重点 / ❓ 疑问 标框 | 正文堆满 emoji |
| 展示失败对话 + 踩坑原因 | 只展示成功对话 |

---

## 示例对话格式

```
**你**：帮我把桌面上所有 PNG 文件列出来。
Claude：我要运行 `ls ~/Desktop/*.png`，是否允许？
[允许一次] [总是允许] [拒绝]
```

- 粗体 `**你**`、常规 `Claude`
- 命令用反引号包
- 权限选项用方括号
- 不编造假回复

---

## 禁止出现

- ❌ 完整 JSON / YAML 配置贴正文（只在附录）
- ❌ shell 脚本超过 3 行
- ❌ "众所周知"、"显而易见"、"很简单"等预设措辞
- ❌ 研究论文引用 / 学术脚注
- ❌ 英文缩写不解释
- ❌ 跨章节引用 ROI 很高的概念不做回顾（第 12 章讲 context，第 13 章再用时要简短带一句）

---

## 术语一致性

写完一章，用 `术语翻译表.md` 对照扫一遍，确保：
- 同一术语全书翻译一致
- 新术语补进翻译表再用

---

## 视觉化优先

- 决策 / 对比 → 表格
- 流程 → mermaid 图或 ASCII
- 快捷键 / 命令 → 代码块
- 界面操作 → 必配截图（Mac + Windows 分别）


## 2026-09 接入补充（优先于旧版冲突条款）

- 第 0 章讲账号和费用，不再承诺国内零成本；模型与版本事实以 facts.yaml 当前登记为准。
- 示例对话、ASCII 界面框明确作示意，不补造真实截图。新图延续已有 forest-v1 mindmap 约定，存量流程图可保留。
- 章节顺序和图示登记唯一源是 book.yaml；assets/diagrams/README.md 仅作维护入口。
- 源稿先保存提交，再记录 checks；只有实际另一引擎结果才完成编辑交叉审，事实核对不替代双平台操作。
- 合订稿、导航、繁体和旧入口页由构建生成。维护流程见 docs/维护构建.md。
