# 读者入口规范 · 0.1.0

书仓 README 顶部说明适合谁、怎么开始、前置与适用版本；中部为可点击目录；底部为状态、许可、反馈和系列入口。作者生产资料放页底 details 折叠区，私有报告不链接出去。

受管区使用 `<!-- studio:toc -->` / `<!-- /studio:toc -->` 与 `<!-- studio:release -->` / `<!-- /studio:release -->`；单元导航使用 `<!-- studio:nav -->` / `<!-- /studio:nav -->`。区外人工文字保留。

当前维护稿与固定里程碑分开；正文版本与 PDF 源版本分开。没有成功公开的下载地址不提前生成。语言落后显示其来源及旧入口，不称全语言同步。

目录唯一来自 book.yaml，图数/章数可计算，不另写一份脚本清单。仅生成导航不修改正文核验状态。
