# 术语翻译表（全书术语用法表；产品事实见 facts.yaml）

> **使用规则**：写正文时遇到术语先查这里。翻译不一致会让零基础读者崩溃。新术语先补进这里再用。

## A-Z 英文术语

| 术语 | 生活化翻译 | 首次解释方式 |
|------|----------|-----------|
| agent | 有特定岗位的 Claude 实习生 | "一个设定了专门角色的小 Claude，比如'代码审查员'" |
| API | 应用程序接口 | "程序之间互相说话的约定" |
| CLI | 命令行工具 | "主要通过文字命令操作的界面" |
| CLAUDE.md | 项目说明书 | "放在项目文件夹里的一份'岗位说明'，按文件位置、会话与当前配置加载" |
| command | 命令 | "给电脑或 Claude 下的一条指令" |
| context | 短期工作记忆 | "模型当前处理任务时参考的材料；容量与相关性都要管理" |
| `/clear` | 开始新对话 | "不沿用刚才的工作上下文；不删除全部历史、不撤销文件，规则与记忆仍可能加载" |
| `/compact` | 整理思路 | "让 Claude 把冗长的对话浓缩成关键要点，腾出记忆空间" |
| diff | 修改对比图 | "文件改动前后差异，可用增删行或并排两栏显示" |
| directory / folder | 文件夹 | 统一用"文件夹"，不用"目录" |
| hook | 自动触发规则 | "设定'当 X 发生，就自动做 Y'的规则" |
| IDE | 集成开发环境 | "把编辑、调试等能力放在一起的软件" |
| install | 安装 | 直接用"安装" |
| JSON | 一种结构化文本格式 | "一种电脑读起来方便的文本记事法" |
| MCP | 外接工具插座 | "让 Claude 能连到它自己电脑之外的工具，比如 Google Drive、Notion" |
| model | 大模型 / AI 模型 | "Claude 背后的大脑，有不同'聪明度'和'速度'可选" |
| pane | 分屏区域 | "窗口里被切分出来的一块区域" |
| path | 路径 | "文件在电脑里的地址写法" |
| PowerShell | Windows 自带的命令行 | "可在终端中运行的命令解释器；窗口与解释命令的程序是两回事" |
| permission | 权限 | 直接用"权限" |
| prompt | 提示词 / 你说给 AI 的话 | 根据语境选 |
| Skill | 技能包 | "可复用的任务手册；按配置由用户主动调用或模型选择" |
| session | 会话 | "一段有记录的工作对话，可以退出后继续" |
| shell | 命令解释器 | "解析并执行命令的程序，例如 zsh、PowerShell" |
| slash command | 斜杠命令 | "以 `/` 开头的快捷指令，比如 `/help`" |
| subagent | 子智能体 | 同 agent，更强调"主 Claude 派出去的小助手" |
| terminal | 终端 | "承载文字输入和输出的界面，Mac、Windows、Linux 都有" |
| token | 模型处理信息的单位 | "与汉字、单词没有固定换算比例；API 费率与订阅额度要分开看" |
| workspace | 工作区 | "一个工作环境，通常对应一个文件夹或一个项目" |
| worktree | 并行工作副本 | "git 的一个功能——让同一个项目能同时存在多份独立副本" |

## 按键与符号

| 符号/按键 | 怎么念 / 怎么写 | 首次出现的解释 |
|---------|------------|-------------|
| `~` | 波浪号 | "代表你自己的用户文件夹，Mac 上是 `/Users/你的名字`" |
| `~/` | 波浪号加斜杠 | "从你的用户文件夹开始的路径" |
| `./` | 点加斜杠 | "当前文件夹" |
| `../` | 两个点加斜杠 | "上一层文件夹" |
| `/` | 正斜杠 | "Mac/Linux 的文件夹分隔符（Windows 是反斜杠 `\`）" |
| `$` | 美元符号 | "Mac 终端的提示符，看到它就说明可以打字了" |
| `%` | 百分号 | "新版 Mac（zsh）的提示符，和 `$` 一样意思" |
| `>` | 大于号 | "Windows PowerShell 的提示符" |
| `|` | 竖线 / 管道符 | "把前一个命令的输出送给后一个命令" |
| `Ctrl + C` | 按住 Ctrl 再按 C | "通常用于请求中断；是否停止成功需查看当前程序状态" |
| `Cmd + C` | 按住 Command 再按 C | "Mac 的复制快捷键（Windows 对应 Ctrl+C）" |
| `Esc` | Escape 键 | "键盘左上角" |
| `Esc Esc` | 连按两次 Esc | "输入为空时打开检查点菜单；有草稿时可能先清草稿，不是立即撤销" |

## 约定

| 中文表达 | 统一用法 | 不用 |
|---------|---------|-----|
| 让 Claude 做事 | "让它..." | "调用它..." |
| 代码修改 | "改" | "修改"（太书面） |
| 运行程序 | "跑" / "执行" | "调用" |
| 建一个 | "建" | "创建" |
| 文件夹里 | "在 xxx 里" | "在 xxx 下" |

---

**维护规则**：新词出现时先在这里登记，再进正文。修改已有术语翻译时必须全书扫一遍替换。


## 2026-09 补充

| 原词 | 本书用法 | 说明 |
|---|---|---|
| Fable 5.1 / Opus 5.5 | 保留模型名称 | 模型名不等于账号套餐或权限模式 |
| effort | 思考投入 / 思考强度 | 和模型、速度模式分别说明 |
| usage credits | 额外付费用量 | 先说明是否在订阅之外计费 |
| Manual / default | 手动模式 / 配置名 default | 不把配置名误当所有套餐默认模式 |
| Auto | 自动权限判断模式 | 不能理解成无限权限或结果无需检查 |
| native install | 原生安装 | 本书主线，不以 Node.js 为前置 |
| Remote Control | 远程控制 | 从其他设备接续，仍由原机器执行 |
| Routines | 云端例行任务 | 与 CLI 循环及桌面本地计划分别说明 |
